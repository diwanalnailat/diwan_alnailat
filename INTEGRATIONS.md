# تحديث مراجعة الجاهزية — 2026-09-15

المرجع الحالي للتشغيل: [OPERATIONS](docs/OPERATIONS.md)، ولتسليم كوديكس: [CODEX_HANDOFF](docs/CODEX_HANDOFF.md)، ولما لم يُربط أو يُختبر بعد: [READINESS](docs/READINESS.md). طبقة سند الآن تشمل البحث والقراءة والتخصيص والحذف والحفظ، والصيانة تشمل التذكيرات ومعالجة الإرسال المتوقف. يسري عزل مشاريع الإعلام على القراءة والتصدير والمرفقات، ويوجد بديل اعتماد لتعارض المصالح. التفاصيل الأقدم أدناه تبقى مرجعًا لتطور النسخة؛ عند التعارض اعتمد الوثائق الحالية أعلاه.

# ديوان النائلات — التشغيل والربط

هذه النسخة تحت بوابة Sites الخاصة. مفاتيح الخدمات غير مضافة، ولم يُرسل OTP أو تنبيه فعلي ولم يُختبر موديل مدفوع. الربط المهيأ مستقل عن حالة تفعيل الخدمات؛ لا يُعرض قبول المزود للرسالة على أنه وصول للمستلم.

## مصدر الهوية والمحتوى

أصول `public/brand` مستخرجة من دليل الهوية الذي رفعه صاحب المشروع: الرمز المنفصل من صفحة 7، التوقيع من صفحة 4، رسومات القافلة من صفحة 25، صورتا الإبل من صفحتي 65 و66. الرموز المستخرجة تحافظ على شكل المصدر، بما فيه الشفافية الأصلية. الصور مقدمة بوصفها صور مكتبة الهوية، وليس إثباتًا لهوية فردية معينة.

سجل النتائج حسب المواسم وأرقام النسخ موجود في [docs/nailat-results.md](docs/nailat-results.md)، مع مراجع كل نتيجة والتعارضات التي لم تُحسم. لا تظهر مصادر في واجهة الزائر، ولا يُدّعى اكتمال أرشيف X. الواجهة تتضمن 33 مركزًا مثبتًا عبر النسخ 6 إلى 10، مع فصل التكريم عن نتائج المزاين.

صورة التكريم الأصلية `public/brand/honoring.jpg`: https://pbs.twimg.com/media/GjiKeVRXwAASaxP?format=jpg&name=orig . منشور النائلات بتاريخ 2025-02-11: https://x.com/alnailat/status/1889412308314829145 ، وتغطية الحفل https://arriyadiyah.com/860071 . صورة `owner-with-camels.png` من صور مكتبة الهوية المقدمة من المستخدم.

## مسار الأعضاء

- الصفحة الرئيسية → طلب الانضمام → رمز واتساب → حساب pending دون وصول تشغيلي → قائمة طلبات الانضمام → 3 خطوات للمراجعة: البيانات، اللجان والصلاحيات، ملخص الوصول → active.
- الإضافة المسبقة تحفظ حسابًا برقم جوال. رمز صحيح لنفس الرقم يربط هوية Site الموثوقة بالسجل المجهز إن لم يكن مرتبطًا بهوية أخرى؛ الحساب active يبقى معتمدًا، invited يصبح pending، suspended يبقى موقوفًا. الربط محمي بشرط ذري يمنع إعادة ربط هوية أخرى.
- وضع «كامل صلاحيات المنصة» يمنح قاعدة واحدة عامة، ويزيل التخصيص والحزم القديمة عند حفظ هذا الاختيار الصريح. لا يتجاوز قواعد منع الاعتماد الذاتي أو تسلسل الاعتمادين. وضع «حسب عضويته» يزيل المنح الإضافية؛ وضع التخصيص يحفظ القواعد والحزم المحددة.
- `POST /api/users/access-preview` يحسب ملخص الوصول على الخادم بنفس دوال التفويض، بما فيها عضويات اللجان والقواعد الحالية المحفوظة. الحالة غير النشطة تمنع الدخول رغم إعداد الصلاحيات. صلاحيات الموردين لم تعد تمنح ضمنيًا لكل حساب؛ تُمنح صراحة.
- `GET /api/auth/status` يعرض حالة صاحب الهوية الموثوقة فقط دون بيانات أشخاص آخرين. المنح العامة وحزم الصلاحيات لا تستطيع تجاوز القيود الصريحة على المُفوِّض. الهوية الموثوقة من بوابة Site ما زالت مطلوبة؛ هذا ليس دخولًا عامًا مستقلًا.

## سَنَد

المسارات: `GET /api/agent/history`, `POST /api/agent/ask`, `POST /api/agent/transcribe`, `POST /api/extract`.

- Responses API مع `store:false`، موديل يحدده `OPENAI_MODEL`، وحد طلبات يومي لكل عضو. أضف حدود إنفاق مستقلة في حساب المزود؛ حد الطلبات ليس سقفًا ماليًا مضمونًا.
- البيانات تحضّر على الخادم من نفس نتائج الصلاحيات التي تستخدمها الواجهة. المجاميع تُحسب على كامل النتائج المرئية قبل تقليص قائمة السجلات. أرقام المال بالهللة داخل الخادم؛ عرض الريال بالقسمة مرة واحدة على 100.
- أداة `prepare_record` تجهّز حقول مهمة/مصروف/مورد/أصل/دليل. تظهر المسودة في النموذج الحقيقي، وتخضع عند الحفظ لكل التحقق والصلاحيات والإسناد والإشعارات الحالية. لا توجد أداة اعتماد مالي أو منح وصول للوكيل.
- الصورة أو PDF خاص في R2؛ الخادم يراجع حق الوصول قبل إرساله إلى المزود. الحقول غير المقروءة تعود null. الواضح فقط يعبّأ، ويظل المستخدم مسؤولًا عن المراجعة ومصدر الدفع والبند. لا يستنتج سَنَد جهة الاعتماد من الفاتورة.
- التسجيل الصوتي حتى 2 دقيقة في الواجهة / 10MB في الخادم. يُفرَّغ إلى نص يراجعه العضو قبل الإرسال. لا يُحفظ ملف الصوت في سجل المحادثة.
- المحادثات محفوظة لكل عضو في D1. بصمة الصلاحيات تمنع إعادة عرض الردود القديمة بعد تغير الوصول. يلزم وضع سياسة الاحتفاظ والحذف قبل الإطلاق العام.
- لا يرسل النظام شيئًا إلى OpenAI قبل `AI_ENABLED=true` وإعداد المفتاح والموديل؛ تظل النماذج اليدوية متاحة.

مراجع الربط: [Function calling](https://developers.openai.com/api/docs/guides/function-calling)، [Structured outputs](https://developers.openai.com/api/docs/guides/structured-outputs). مزود الصوت يستخدم `POST https://api.openai.com/v1/audio/transcriptions` مع موديل تفريغ يحدده الإعداد.

## واتساب وOTP

املأ مفاتيح المثال من حساب Meta Business الخاص بالجهة. استخدم إصدار Graph API المدعوم في الحساب في `WHATSAPP_API_VERSION` دون تثبيت إصدار قديم داخل الكود.

1. اعتمد قالب **Authentication / Copy code**. إرسال الرمز: body parameter 1 ثم URL button index 0 بنفس الرمز. مراجع [قوالب المصادقة](https://developers.facebook.com/documentation/business-messaging/whatsapp/templates/authentication-templates/authentication-templates) و[زر نسخ الرمز](https://developers.facebook.com/documentation/business-messaging/whatsapp/templates/authentication-templates/copy-code-button-authentication-templates).
2. اعتمد قالب **Utility** بثلاثة معاملات نصية بالترتيب: اسم العضو، عنوان التنبيه، رابط السجل. اجعل الرسالة مختصرة: «يا {{1}}، {{2}} في ديوان النائلات. التفاصيل والمتابعة: {{3}}». التصنيف والقبول النهائيان لدى Meta.
3. `APP_BASE_URL` عنوان HTTPS للديوان؛ يستعمله التنبيه لفتح السجل ثم يعيد الخادم التحقق من الوصول. لا تُرسل مبالغ أو مرفقات في القالب.
4. لا يرسل التنبيه دون عضوية نشطة وجوال موثق وموافقة تنبيهات. يمكن إيقاف التنبيهات من «مساحتي». طلب OTP له موافقة مستقلة.
5. كل عملية عمل تولد outbox دائمًا. عند توفر إعدادات المزود تعالج الاستضافة الدفعة بعد نجاح الطلب (`waitUntil`). يمكن تشغيل `POST /api/integrations/dispatch` من إعدادات الربط بصلاحية إدارة الإعدادات. في الاستضافة المستقلة يُربط بمجدول كل دقيقة لضمان معالجة retries دون انتظار نشاط المستخدم. الدفعة 20، retry للحد المؤقت فقط وبحد 4؛ أخطاء النقل غير المؤكدة تحتاج مراجعة لتجنب إرسال مكرر. سجل `sending` المتروك بسبب توقف العامل يحتاج تسوية تشغيلية قبل إعادة الإرسال.
6. webhook جاهز على `/api/webhooks/whatsapp`: تحدي التحقق + HMAC SHA256 لتوقيع الجسم الخام. يحدّث حالات sent/delivered/read/failed للمعرف الصادر فقط، مع منع تراجع التسلسل. لا يفسر الردود الواردة على واتساب كأوامر مالية؛ رابط التنبيه يفتح محادثة المهمة داخل الديوان.
7. **بوابة Sites الخاصة تمنع Meta من استدعاء webhook مباشرة.** يجب استضافة webhook على عنوان قابل للوصول من Meta، أو نقل التطبيق عند الإطلاق إلى استضافة مناسبة. لا تُجعل مساحة العمل عامة لمجرد قبول webhook.

OTP: `/api/auth/request` و`/api/auth/verify`. صلاحية 5 دقائق، 5 محاولات، إعادة بعد 60 ثانية، حد بالهوية وبالرقم. توليد آمن، HMAC بسر ≥32 حرفًا، استعمال مرة واحدة، لا يوجد رمز تجريبي أو backdoor. الأكواد لا تسجل في logs ولا ترجع في الرد.

**المتاح الآن هو توثيق الجوال وربطه بهوية الدخول الموثوقة في Sites.** التسجيل الجديد بعد التوثيق ينشئ حسابًا pending بلا أي grants حتى موافقة الأدمن. ربط سجل جوال سبق إنشاؤه بهوية Sites أخرى يحتاج معالجة الأدمن؛ لا تُنقل الهوية بمجرد امتلاك الرقم. تسجيل دخول مستقل برقم الجوال وحده يتطلب طبقة جلسات التطبيق عند الانتقال للاستضافة العامة، واستعادة الحساب وتفويض الأجهزة ومراجعة أمنية. لم نضع مسارًا يلتف على بوابة الاستضافة الخاصة.

## الاعتمادات

- المشروع لا يحدد معتمدًا ماليًا. إنشاء لجنة يختار مديرًا ومعتمدًا ثانيًا ممن يمتلكون expenses:view + expenses:approve ضمن نطاقها.
- المسمى الوظيفي لا يمنح سلطة. محرر العضو يضيف قواعد لمشروع أو لجنة؛ الدفع `expenses:pay` منفصل.
- لا يجوز نفس الشخص للاعتمادين. الإسناد إلى لجنة لا يتجاوز المنع الصريح. تغيير المسار يعيد مراجعة الطلب المعلق دون حذف سجل الموافقات السابقة.
- تحويل إعدادات النسخة السابقة يحدث مرة واحدة عند دخول أدمن: تتحول سلطة المشرف السابق إلى grants صريحة بنفس النطاق، ويُنقل اسمه إلى اللجان ثم يُحذف الربط القديم بالمشروع، ضمن batch محمي بإصدارات السجلات وسجل تدقيق.
- نسخ مشروع سنوي يحتفظ بالهيكلة وبنود الصرف دون المبالغ أو الأفراد أو المعتمد الثاني أو المهام.

## حدود الجاهزية والاختبار

اختبارات محلية تستعمل مزود HTTP بديلًا محقونًا في بيئة الاختبار، وتفحص طلبات المزود والرفض والصلاحيات. لا يعني نجاحها قبول قوالب Meta أو جودة موديل حقيقي. قبل الإطلاق: تهيئة الأسرار، تجربة OTP، رسائل فعلية وتحديث webhook، اختبار استخراج فواتير عربية حقيقية، تجربة استرجاع النسخ الاحتياطية، وفحص أمن مستقل. تطبيقات iOS/Android والمتاجر مرحلة بناء مستقلة؛ هذه نسخة ويب متجاوبة.

## التوثيق الميداني للأصول والمواقع

- للأصل رقم تعريف فريد في serial، مستقل عن رقم الشركة المصنعة الاختياري. المسح يبحث في السجلات المسموح بها فقط؛ لا يفتح روابط خارجية تلقائيًا. تطابق رقم الشركة المصنعة مع عدة سجلات لا يختار أصلًا عشوائيًا.
- قارئ ZXing Browser 0.1.5 مستضاف محليًا ويُحمّل عند الطلب لقراءة الباركود وQR من الكاميرا أو صورة. قارئ USB الذي يدخل نصًا يعمل مع حقل البحث اليدوي. الإغلاق والإيقاف ينهيان مسار الكاميرا.
- تنزيل ملصق QR بصيغة SVG للطباعة؛ الرمز يفتح السجل بعد الدخول والتحقق من الصلاحيات. الصور محفوظة بالمرفقات الحالية في التخزين الخاص، ويعرض سجل الأصل معرض صور.
- الموقع الاختياري للأصول والمهام والمشاريع محفوظ في meta.site_location: label, notes, lat, lng. الخادم يتحقق من نطاق الإحداثيات واكتمال الزوج. لا يوجد تتبع حي للأشخاص. الإحداثيات اليدوية بديل عندما يمنع المتصفح أو حاوية العرض إذن الجهاز. رابط الاتجاهات يفتح خرائط Google بقرار المستخدم دون API مدفوع.
- حقول الشركة المصنعة والعلامة التجارية والضمان والموقع تدخل في تصدير الأصول. إحداثيات المهام والمشاريع تدخل في تقاريرها أيضًا. لم يُختبر المسح بكاميرا هاتف فعلية داخل بيئة التطوير؛ اختبار الحفظ والصلاحيات والبحث اليدوي يغطي المنطق البرمجي.

## المراجعة 13 — العلاقات الإعلامية والموقع
- دليل العلاقات الإعلامية ملف دائم منفصل في التنقل عن الأصول، مبني على سجل جهة التعامل الحالي (صلاحيات الموردين للإضافة/المشاهدة/التعديل/التصدير)، ولا ينشئ حساب دخول للشخص.
- يحتفظ الملف بالحسابات وعدد المتابعين وتاريخ الرصد، ومشاركات الأعمال المرتبطة بمشاريع المواسم، وروابط النشر والمشاهدات والتفاعلات والمرفقات وتقييم التعاون. الأعداد إدخال موثق وليست مزامنة اجتماعية آلية. لا تجمع متابعي الحسابات كمجموع جمهور فريد.
- ملفات العمل في R2 مع صلاحيات المستندات الحالية، والروابط وحدها لا تعتبر نسخة أرشيفية. الحد الحالي 10 MB لكل ملف، حتى 10 ملفات جديدة في الحفظ؛ PDF وصور وMP4/WebM. لا تزال أذونات قراءة مستند المورد تتطلب تعديل المورد حفاظًا على المستندات النظامية والمالية الموجودة.
- الأصول تبقى بسجلاتها وحركات التسليم والاسترداد عبر المواسم؛ العلاقات الإعلامية ليست أصولًا مادية. لا تُنسخ الملفات الإعلامية عند استنساخ هيكل المشروع.
- خريطة اختيار مشتركة للمورد والأصل والمهمة والفعالية، مكتبة Leaflet محلية وبلا مفتاح. تحميل خرائط OpenStreetMap يحدث عند فتح الخريطة فقط، مع نسبة المصدر، HTTPS، وإحالة origin وسياسة cache الافتراضية. الخدمة تتطلب اتصالًا بالإنترنت ولا توفر ضمان توافر؛ لا تنزيل جماعي أو خرائط offline.
- مصدر صورة قسم المشاركة البديلة: ملف المستخدم «توثيق مشاركة منقية النايلات.pdf»، الصفحة 1، مع الحفاظ على حقوق الصورة المضمنة. حساب النائلات المثبت أسفلها https://x.com/alnailat . تعذر تنزيل الصور من X؛ لم ننسب الأشخاص إلى العائلة.
- المصروف لا يسمح لعضو عادي برفعه للجان خارج عضويته، ولا ربطه بمهمة غير مسندة إليه. مدير اللجنة ومن يملك صلاحية الإسناد أو إدارة المستخدمين يحتفظ بوصوله المفوض. تم إصلاح اختيار اللجنة الفعلية للطلب عند تعديله.

## المراجعة 14 — الصورة وواجهة سند
- صورة قسم الحضور الحالية مستخرجة من دليل الهوية البصرية للنائلات، الصفحة 67، بدقة 1211 × 766. تُعرض كاملة بنسبة أبعادها الأصلية دون قص، مع تعليق مختصر أسفلها.
- رابط حساب X أصبح في تذييل الصفحة فقط.
- زر سند يستخدم علامة الهوية الأصلية، ونافذة المحادثة أعيد ترتيبها: رأس واضح، سياق العمل، المحادثة، الاقتراحات، صندوق الكتابة، ثم أدوات قابلة للفتح. لم يتغير وضع ربط خدمة الذكاء الاصطناعي.

## المراجعة 15 — تنويع صورة الحضور
استُبدلت صورة القطيع الجوية الثانية بلقطة قريبة للوضح من دليل الهوية، الصفحة 65، بدقة 1375 × 778؛ العرض يحافظ على الصورة كاملة ونسبتها الأصلية. بداية الصفحة وحدها تحتفظ بلقطة القطيع الجوية.

## المراجعة 16 — عبارة الصورة
أضيفت عبارة «شغفٌ يُورث. وعنايةٌ تُثمر.» و«قريبون من إرثنا. ماضون بعزيمتنا.» كطبقة نص قابلة للقراءة فوق الصورة الحالية، مع تدرج لرفع التباين دون قص الصورة أو تعديل ملفها.
